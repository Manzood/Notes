#include<bits/stdc++.h>
using namespace std;

//basically, create a file called input and another file called output for yourself
//then go to view, and make it so that 3 columns are visible
//put input and output in the second and third column
//then go to groups, and select limit to two columns
//slide columns to your preferred size, and that's it!
//also, copy the code snippet as needed
//press control shift b to run the first time, and then on subsequent runs you can press control+b only

int main () {
	#ifndef ONLINE_JUDGE
		freopen("input","r",stdin);
		freopen("output","w",stdout);
	#endif

	int x;
	cin>>x;
	cout<<x+20;
}
